source 'https://rubygems.org'
# Need to uncomment below when deploying to Heroku
#ruby '1.9.3'

gem 'rails', '3.2.2'

gem 'json'

group :development do
  gem 'sqlite3'
end

group :production do
  gem 'pg'
end

gem 'thin'

gem 'foreman', '0.47.0'

gem 'haml'

gem 'ruby-hmac'

gem 'fastercsv'

gem 'nokogiri'

gem 'bootstrap-sass', '2.0.0'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'
  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'
gem 'rails3-jquery-autocomplete', '~> 1.0.7'
gem 'google-analytics-rails'
# I use newrelic on the live install; commented out here
#gem 'newrelic_rpm'
gem 'carmen'
gem 'settingslogic'


# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'
